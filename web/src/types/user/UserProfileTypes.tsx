export type UserProfile = {
  myProfile: {
    id: string;
    email: string;
    lastName: string;
    firstName: string;
  };
};

export type UserProfileVariables = {
  id: string;
  email: string;
  lastName: string;
  firstName: string;
};

